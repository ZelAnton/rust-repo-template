//! TODO: crate-level docs — describe what this binary does.

fn main() {
    println!("Hello from the Rust repo template — replace me with your app.");
}

#[cfg(test)]
mod tests {
    #[test]
    fn arithmetic_holds() {
        assert_eq!(2 + 2, 4);
    }
}
